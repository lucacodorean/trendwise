# GitHub Issue Drafts

These issue drafts are derived from `docs/prd-stock-predictor-prototype.md`, `docs/tasks-v1.md`, `docs/product-v1.md`, `CONTEXT.md`, and the accepted ADRs.

Publish them to `lucacodorean/trendwise` in numeric order so dependency references line up with the intended issue sequence. Apply the `ready-for-agent` label to each issue.

The current environment cannot publish them directly because the `gh` CLI is not installed.
